import { makeStyles } from '@material-ui/core/styles';
const useStyles = makeStyles(
    theme => ({
      root: {
        fontWeight: 'bold',
        padding: theme.spacing(1),
        margin: theme.spacing(1),
      },
  
      content: {
        color: 'red',
        fontWeight: 'bold',
        margin: '5rem',
      },
      black: {
        color: 'black'
      }
    }),
    { name: 'App' }
  )
  const PressedChatButton = () => {
return(
    <div>Here</div>
)
  }
  export default PressedChatButton